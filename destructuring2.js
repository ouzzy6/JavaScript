const person = {
    id: 1,
    firstName: 'Mario',
    lastName: 'Rossi',
    age: 25
  };
  
  // Destructuring en una línea
  const { id, firstName, lastName, age } = person;
  
  console.log(id, firstName, lastName, age); 