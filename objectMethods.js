let person = {
    firstName: 'Mario',
    lastName: 'Rossi',
    age: 25
}

let keys = Object.keys(person);
console.log(keys)

let values = Object.values(person);
console.log(values);

//En este ejemplo usamos .keys y .values para devolver los resultados.